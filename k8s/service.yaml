apiVersion: v1
kind: Service
metadata:
  name: auto-deploy-api
  namespace: auto-deploy
spec:
  type: ClusterIP
  selector:
    app: auto-deploy-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
